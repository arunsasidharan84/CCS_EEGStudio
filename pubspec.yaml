name: ccs_eeg_app
description: Lightweight EEG feature extraction and connectivity desktop app.
publish_to: none
version: 1.1.3+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^11.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
